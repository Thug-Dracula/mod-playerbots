All evidence gathered. Here is the exhaustive Scope B cross-reference.

FILE KEY
- OG = C:/Users/Admin/git/scratch/cmangos-playerbots/playerbot/TravelNode.{h,cpp}
- VEN = C:/Users/Admin/git/azerothcore-wotlk/modules/mod-cmangosbots/src/playerbot/TravelNode.{h,cpp} (header is byte-identical to OG except include swap `MotionGenerators/PathFinder.h`→`PathGenerator.h`; .cpp deltas are ONLY those listed — verified by whitespace-insensitive diff, 463 diff lines total, all catalogued below)
- TGT = C:/Users/Admin/git/main/azerothcore-wotlk/modules/mod-playerbots/src/Mgr/Travel/TravelNode.{h,cpp} (branch feature/new_rpg_and_nav_5_bash)

================================================================
SCOPE B ENTRIES — TravelPath walker
================================================================

PathNodeType enum | OG h:250 | VEN h:250 | NONE | PORTED-FAITHFUL (TGT h:417; comment h:413 documents value 3=NODE_AREA_TRIGGER (was NODE_PORTAL), 7=NODE_STATIC_PORTAL (was NODE_FLYING_MOUNT) — re-flipped to cmangos values) | — | Consumed by walker + HandleSpecialMovement dispatch. TGT's TravelNodePathType still diverges: value 2 named `portal` (OG `areaTrigger`), extra `flyingMount=7` (OG absent).

PathNodePoint::operator== | OG h:268 | VEN h:268 | NONE | PORTED-FAITHFUL (TGT h:435) | WorldPosition op== | Identity for cutTo std::find.
PathNodePoint::isWalkable | OG h:270 | VEN h:270 | NONE | PORTED-FAITHFUL (TGT h:443, extra explanatory comment) | — | `type <= NODE_NODE`.

TravelPath ctors | OG h:277-279 | VEN h:277-279 | NONE | PORTED-FAITHFUL (TGT h:450-460) | — | —
TravelPath::addPoint (both) | OG h:281-282 | VEN same | NONE | PORTED-FAITHFUL (TGT h:462-468) | — | —
TravelPath::addPath (both) | OG h:283-284 | VEN same | NONE | PORTED-FAITHFUL (TGT h:469-479) | — | —
TravelPath::clear/empty | OG h:285/287 | VEN same | NONE | PORTED-FAITHFUL (TGT h:480/482; empty() now const) | — | —
TravelPath::getPath (mutable ref) | OG h:291 | VEN h:291 | NONE | PORTED-FAITHFUL (TGT h:503) | — | TGT additionally has GetPath() by-value (h:485) and GetPathRef() const (h:486) — modpb-only helpers used by the evolved executor.
TravelPath::getFront/getBack | OG h:292-293 | VEN same | NONE | PORTED-FAITHFUL (TGT h:487-488) | — | UB on empty path in all three (front() on empty vector); callers guard.
TravelPath::getPointPath | OG h:295 | VEN h:295 | NONE | PORTED-FAITHFUL (TGT h:490) | — | —
TravelPath::size()/operator[] | ABSENT | ABSENT | — | TGT-only (h:483-484) | — | Used by modpb TravelPlan executor (stepIdx walk). Keep; harmless additive.

TravelPath::cutTo | OG cpp:800 | VEN cpp:804 | NONE (MANGOS_ASSERT→ASSERT only) | PORTED-FAITHFUL (TGT cpp:825) | PathNodePoint::==, ASSERT | Erases prefix; asserts we never cut in the middle of one flight-path leg (same entry both sides). Semantics identical in all three.

TravelPath::makeShortCut | OG cpp:821 (void) | VEN cpp:825 (void) | NONE | DIVERGED-minor (TGT cpp:720, returns bool; bot param defaulted nullptr in header h:498) | sPlayerbotAIConfig.tooCloseDistance, WorldPosition::sqDistance/distance/getPathTo/isPathTo, isWalkable | Algorithm is line-for-line faithful (same-map+walkable anchor filter, tooCloseDistance on-path reset, NODE_PREPATH skip, minDist/totalDist/2 heuristic, maxDistSq reject→clear(), firstNode/tooClose keep-old, on-path adopt, else path to new begin and prepend). Only delta: bool return (callers at MovementActions.cpp:4126 ignore it) and comments. Assessment: keep TGT; bool is additive.

TravelPath::shouldMoveToNextPoint | OG cpp:901 | VEN cpp:905 | NONE | PORTED-FAITHFUL (TGT cpp:847) | PathNodeType semantics, WorldPosition::distance | All stop rules identical: end/next-end guards; AT-pair, static-portal-pair, teleport-pair (same entry) stop; next-is-transport-with-entry stop; on-transport-node skip-if-not-using (prev/next not transport && p!=beg) else stop; flight-pair stop; map-change or (moveDist+next>maxDist or startPos→next>maxDist)&&moveDist>0 stop; else accumulate moveDist.

TravelPath::getNextPoint | OG cpp:964 (private h:304) | VEN cpp:968 (private) | NONE | PORTED-FAITHFUL (TGT cpp:918; visibility raised to public h:513) | shouldMoveToNextPoint, projectOnSegment, isWalkable | Same three phases: closest same-map walkable point (skipped when onTransport), advance while shouldMoveToNextPoint, projectOnSegment (0,1) → prefer nextP. Public visibility is additive (dispatch layer peeks); keep.

TravelPath::UpcommingSpecialMovement | OG cpp:1022 | VEN cpp:1026 | ADAPTATION(marked): OG's `IsPointInAreaTriggerZone(atEntry, …, 0.5f)` proximity gate replaced with `if (false)` (dead — marked "AC equivalent … deferred; dead while the sAreaTriggerStore shim is empty"); `sObjectMgr.GetAreaTrigger`→`GetAreaTriggerTeleport` | DIVERGED-minor (TGT cpp:979): AT branch validates only `sObjectMgr->GetAreaTrigger(entry)` non-null; the DBC point-in-box geometry check is omitted with comment "no AC equivalent and was already dead upstream" | sPlayerbotAIConfig.transportTeleportType, INTERACTION_DISTANCE, cutTo, getNextPoint, sObjectMgr AreaTrigger | Behavior: OG requires the bot to be physically inside the AT box before cutting to the activation point; VEN/TGT both drop this (VEN dead-coded, TGT omitted). Consequence: the walker signals "special movement" for AT legs from any distance — the dispatch layer must handle approach itself. Note VEN keeps the OG null-`atEntry` return path via its shim; TGT restructured to a single server-side AreaTrigger lookup. Assessment: acceptable, BUT for full fidelity the AC equivalent exists (`sObjectMgr->GetAreaTrigger(entry)` has x/y/z/radius/box fields — AC's `IsWithinAreaTriggerZone`-style check in AreaTrigger handler could be replicated); flag as a portable gap rather than impossible. All other branches (staticPortal INTERACTION_DISTANCE, teleport nextP, flightpath INTERACTION_DISTANCE, transportTeleportType<2 walk-on path incl. entry-change scan, ==2 teleport-to-end) faithful in TGT.

TravelPath::ClipPath | OG cpp:1118 | VEN cpp:1125 | DEVIATION(unmarked)×3: (1) `unit->GetAttackDistance(mover)` → `unit->ToCreature() ? …GetAttackDistance : 0.0f` (non-creature range collapses to 0 — harmless, targets filtered to creatures); (2) `CanAttackOnSight` → `ToCreature()->CanStartAttack(mover)` — AC CanStartAttack ALSO checks aggro distance/civilian/assistance, i.e. stricter than cmangos' pure hostility+visibility test, so VEN clips less often; (3) IsDead→isDead casing. Order retained: `cutTo(*startP)` BEFORE the `startP == end()` check — OG's latent bug (deref of end() when path empty after getNextPoint; stale-iterator early-return mid-route) is inherited by VEN | DIVERGED (TGT cpp:1074): (a) end-check moved BEFORE cutTo + fullPath.empty() guard after — fixes the OG/VEN latent UB (documented in comment, "the observed 243y dispatches"); (b) enemy-target scan and hazard scan are STUBBED — commented out verbatim as "PORT-TODO (Phase B)" pending `IsStateActive/BotState`, `possible attack targets` value (AI_VALUE_LAZY), `hazards` value + HazardPosition type (HazardsValue.h); only reactDistance / !isWalkable / >125 sq-dist discontinuity clipping runs | OG deps: AiObjectContext, AI_VALUE_LAZY "possible attack targets", AI_VALUE "hazards" (HazardPosition = pair<WorldPosition,float>), BotState::BOT_STATE_COMBAT, GetAttackDistance, CanAttackOnSight, IsWithinLOSInMap, level+5 filter, sPlayerbotAIConfig.reactDistance | KEY GAP for the full-fidelity port: hazard+enemy clipping is the path-safety layer; port requires HazardsValue and PossibleAttackTargetsValue (Scope: values subsystem). When porting the enemy scan choose CanStartAttack deliberately or replicate cmangos `CanAttackOnSight` (AC equivalent ≈ `IsHostileTo && CanSeeOrDetect`, without range) — VEN's substitution silently changed clip behavior. Keep TGT's check-before-cut fix.

TravelPath::print | OG cpp:1205 | VEN cpp:1212 | NONE | PORTED-FAITHFUL (TGT cpp:809) | GetTimestampStr, printWKT | Debug WKT dump.

TravelPath::IsPathCheating | ABSENT (inline inside OG buildPath cpp:316-340) | ABSENT (inline VEN cpp:320-344) | — | TGT-only extraction (cpp:687, h:525) | — | Same thresholds as OG's inline checks (2-point >5y; >10y vert with >2:1 slope at both ends). Keep: pure refactor, reused by probe validation.

TravelPath::getPathLength-type helpers | not on TravelPath in any repo | — | — | — | `WorldPosition::getPathLength(path)` is what generateTaxiPaths uses (OG cpp:3006, TGT cpp:2636) | Scope item resolved: "size helpers" = TGT's size()/operator[]/GetPathRef additions listed above.

================================================================
TravelNodePath (edge payload)
================================================================

TravelNodePath ctor/copy-ctor | OG h:56/61 | VEN same | NONE | PORTED-FAITHFUL (TGT h:109/126) | — | TGT `pathObject` narrowed uint64→uint32 (h:200); LoadNodeStore still reads Get<uint64> then narrows (cpp:3127). AT ids/GO entries/taxi ids/spell ids all fit u32; benign but note for DB schema parity.
getComplete/getPath/getPathType/getPathObject/getDistance/getSwimDistance/getExtraCost/getMaxLevelCreature/setCalculated/getCalculated/setComplete/setPath/setPathAndCost/setPathType/setPathObject | OG h:66-89 | VEN same | NONE | PORTED-FAITHFUL (TGT h:140-171; getPath renamed GetPath) | — | getComplete forces true for non-walk types in all three.

TravelNodePath::print | OG cpp:22 | VEN cpp:23 | NONE | PORTED-FAITHFUL (TGT cpp:26) | — | Store-format serializer.

TravelNodePath::calculateCost | OG cpp:38 | VEN cpp:39 | ADAPTATION: cInfo field casing, sFactionTemplateStore faction field, extra parens on the water condition (no semantic change — `&&` already bound tighter in OG) | DIVERGED-minor (TGT cpp:43): (1) friendToAlliance/friendToHorde replaced by `Unit::GetFactionReactionTo(entry, factionTemplate 1/2) > REP_NEUTRAL` — approximately equivalent; (2) OG counts lastPoint-in-water swim distance EVEN in distanceOnly mode (precedence quirk: `(!distanceOnly && A) || B`); TGT requires !distanceOnly for both — cleaner but not OG-faithful; (3) OG guards isInWater with isVmapLoaded(); TGT calls isInWater unconditionally (AC loads on demand — fine); (4) OG wraps body in try/catch(...) — TGT none | getCreaturesNear(50), sObjectMgr GetCreatureTemplate, sFactionTemplateStore | Affects stored swimDistance during generation only.

TravelNodePath::getCost | OG cpp:104 | VEN cpp:105 | ADAPTATION(marked)×2: AT conditionId gate dead-coded (`if (map && false)` — AC AreaTrigger row has no conditionId/IsConditionSatisfied); staticPortal GO faction read via GameObjectTemplateAddon. Unmarked: BotGetTeam shim, GetSpellTargetPosition(EFFECT_0) | DIVERGED (TGT cpp:106, signature Player* vs Unit*): MISSING vs OG — (a) Outland gate `path.back().mapid==530 && level<58 → -1` and Northrend gate `571 && level<68 → -1`; (b) areaTrigger conditionId check (dead in VEN too); (c) staticPortal GO-faction hostility check (`GetFactionReaction < REP_NEUTRAL → -1`); (d) spell-click flightpath fallback (`CanSpellClick(bot, pathObject)` when no TaxiPathEntry — OG cpp:176). ADDED vs OG: flyingMount branch (level≥70/CanFly/MOVE_FLIGHT). Retained faithfully: taxi price vs cGold, IsTaximaskNodeKnown, MountCreatureID per team, run/swim speeds, spell 1066 (Aquatic Form) swim×1.5, mob/faction annoyance modifiers (0.1/0.3 per level over -10), walk vs extraCost formula | sTaxiPathStore/sTaxiNodesStore, m_taxi, isTaxiCheater, FactionTemplate | For full fidelity port (a),(c),(d); (a) matters most — without it low-level bots can route onto Outland/Northrend edges (portal edges aren't level-gated elsewhere).

TravelNodePath::getPrice | OG cpp:219 | VEN cpp:223 | NONE | PORTED-FAITHFUL (TGT cpp:187) | sTaxiPathStore | —

================================================================
TravelNode (graph node)
================================================================

ctors/setLinked/setPoint/getName/getPosition/getPaths/getLinks/isImportant/isLinked | OG h:139-153 | VEN same | NONE | PORTED-FAITHFUL (TGT h:222-249) | — | —
isTransport/getTransportId/isPortal | OG h:154-156 | VEN same | NONE | PORTED-FAITHFUL (TGT h:251-277; isPortal tests `portal||staticPortal` = OG's `areaTrigger||staticPortal` numerically) | — | getTransportId returns `false` (0) when none in all three.
isWalking | ABSENT | ABSENT | — | TGT-only (h:279) | — | Additive helper.
getAreaTriggerId | OG cpp:236 (h:157) | VEN cpp:240 | ADAPTATION: GetAreaTriggerTeleport + m_mapId; works only where sAreaTriggerStore shim is populated | ABSENT in TGT | sAreaTriggerStore, position==inPos(z-4) match | Needed by OG isUselessLink AT-block, buildPath cheat check, generateHelperNodes filters. TGT substitutes isPortal() where it needs it.
isAreaTriggerTarget | OG cpp:256 (h:158) | VEN cpp:260 | ADAPTATION: GetAreaTriggerTeleport | ABSENT in TGT | sAreaTriggerStore rows, target_* fields | Needed by OG isUselessLink AT-exit pruning.
mapid/x/y/z/o/getDistance×2/fDist×2 | OG h:161-169 | VEN same | NONE | PORTED-FAITHFUL (TGT h:289-303) | WorldPosition | —
setPathTo | OG h:171 | VEN same | NONE | PORTED-FAITHFUL (TGT h:305) | — | `paths[node]=path; if(isLink) links[node]=&paths[node]` — pointer into unordered_map value; rehash-stability relied on (node* keys, values stable in libstdc++/msvc unordered_map — true, buckets hold nodes).
hasPathTo/getPathTo/hasCompletePathTo | OG h:172-174 | VEN same | NONE | PORTED-FAITHFUL (TGT h:321-332) | — | getPathTo operator[] inserts default path if absent (all three).
buildPath / BuildPath | OG cpp:281 (h:175) | VEN cpp:285 | NONE (translation only) | DIVERGED (TGT cpp:204, renamed BuildPath): faithful skeleton (forward pathfind via getPathFromPath, walk→portal/transport 20y cheat with midpoint, cheat-path rejects, persist partial, recursive reverse build, reverse-flip salvage, <5y stitch, setLinkTo on success, calculateCost(!postProcess)) with 4 deltas: (1) cheat gate uses `!isPortal()/endNode->isPortal()` instead of OG's `!getAreaTriggerId()/endNode->getAreaTriggerId()` (positional AT match) — broader trigger; (2) short/steep checks extracted to IsPathCheating (same thresholds); (3) TGT-only transport-water guard (isTransport && 2nd point in water → fail, "remove when transports properly handled"); (4) TGT-only reverse-mirror block (cpp:315-328): on forward success force-writes reversed forward path + reverse LINK when endNode lacks one — OG deliberately leaves one-way links (cliff drops!) so this REPLACES intended one-directional semantics. Assessment: (1)-(3) keep or align opportunistically; (4) REPLACE with OG behavior for fidelity — creates un-validated reverse links | getPathFromPath, isPathTo, setLinkTo, calculateCost | OG note: mutates the shared graph; OG calls it under shared lock from buildPath-route path (race in OG) — TGT gates all runtime rebuild on non-null mover (see buildPath route entry), a genuine fix; keep.
setLinkTo | OG h:177 | VEN same | NONE | PORTED-FAITHFUL (TGT h:336) | — | TGT BuildPath calls `setLinkTo(endNode, true)` — bool→float 1.0f distance; harmless (path exists so distance arg unused).
hasLinkTo/linkCostTo/linkDistanceTo | OG h:187-189 | VEN same | NONE | PORTED-FAITHFUL (TGT h:347-358) | — | linkCostTo==linkDistanceTo (both return distance) in all three.
removeLinkTo | OG cpp:390 (h:190) | VEN cpp:394 | NONE | DIVERGED-trivial (TGT cpp:334): remove-all branch passes `removePaths` through to other nodes (OG passes the same var — identical); only sTravelNodeMap→instance() | — | —
isEqual | OG cpp:691 | VEN cpp:695 | NONE | PORTED-FAITHFUL (TGT cpp:595) | getNodes() global scan | Unused at runtime in TGT (generation-era helper).
isUselessLink | OG cpp:450 | VEN cpp:454 | NONE | DIVERGED (TGT cpp:391): MISSING (a) whole AT block — if node has getAreaTriggerId and a nearby (<20y) link is an AT-target while farNode isn't → useless (prunes redundant AT-exit links); (b) `nearNode->getAreaTriggerId()` skips (×2 — "node might get removed later"); (c) `hasRouteTo(farNode, mapOnly=true)` pre-gate before route probe; (d) far-path-passes-near-node check (`nearPos.closestSq(farPath->getPath()).distance < INTERACTION_DISTANCE → useless`). Present: walk-only gate, farLength fallback, one-way skip (`farNode->hasLinkTo(this) && !nearNode->hasLinkTo(this)`), direct 1.1 detour test, route-based 1.1 test with hasNode(this) exclusion | getRoute/GetNodeRoute(nullptr), getTotalDistance, closestSq (OG) | Generation-only path in TGT (removeUselessPaths). REPLACE with full OG version when porting generation parity; needs getAreaTriggerId/isAreaTriggerTarget ports.
cropUselessLink (single) | ABSENT | ABSENT | — | TGT-only (cpp:438) | — | Thin wrapper.
cropUselessLinks | OG cpp:538 | VEN cpp:542 | NONE | DIVERGED (TGT cpp:444): OG removes BOTH directions on each hit (`this->removeLinkTo(farNode); farNode->removeLinkTo(this)` in branch 1 and mirrored in branch 2); TGT removes only ONE direction per branch. Changes pruning convergence and can leave asymmetric leftovers | removeLinkTo, isUselessLink, "crop.csv" log | REPLACE with OG both-direction removal for fidelity.
getNodeMap | OG cpp:412 (h:199, has mapOnly param) | VEN cpp:416 | NONE | DIVERGED-minor (TGT cpp:358): mapOnly parameter dropped (OG uses it for hasRouteTo(mapOnly) in isUselessLink + removeUselessPathMap route clearing) | links BFS | Re-add mapOnly when porting full isUselessLink.
hasRouteTo | OG h:202 (mapOnly param) | VEN same | NONE | DIVERGED (TGT h:373): mapOnly dropped; lazy directed-BFS fill retained as fallback, but at runtime `routes` is prefilled by PrecomputeReachability with UNDIRECTED components (TGT-local change, see below) | getNodeMap, routes map | See PrecomputeReachability assessment.
getRouteSize | OG h:203 | VEN same | NONE | ABSENT in TGT | — | Only consumer is OG generateHelperNodes (absent in TGT).
clearRoutes | OG h:204 | VEN same | NONE | PORTED-FAITHFUL (TGT h:382) | — | TGT adds setRouteTo (h:383) for PrecomputeReachability.
TravelNode::print | OG cpp:714 | VEN cpp:718 | NONE | DIVERGED-trivial (TGT cpp:618): pathType CSV code simplified (casts enum, only 0-if-broken; OG maps to 0-7 QGIS categories incl. same-map-AT=3/cross-map=4); drops node names columns | "travelNodes.csv"/"travelPaths.csv" | Debug-only.

PortalNode (class) | OG h:233-247 | VEN same | NONE | ABSENT in TGT | TravelNode, teleportSpell path type | Temp A*-injection node for hearthstone (8690) + mage teleports. Required for OG getRoute fidelity (below).

================================================================
TravelNodeRoute + A* stub
================================================================

TravelNodeRoute ctor (nodes, tempNodes) | OG h:314 | VEN same | NONE | DIVERGED (TGT h:539): tempNodes parameter/member REMOVED | — | See temp-nodes entry.
isEmpty/hasNode/findNode | OG h:316/318/331 | VEN same | NONE | PORTED-FAITHFUL (TGT h:544/546/570) | — | —
setNodes | OG h:321 | VEN same | NONE | ABSENT in TGT | — | Unused by TGT resolvers.
getNodes | OG h:322 (by-ref) | VEN same | NONE | DIVERGED-trivial (TGT h:552, by-value copy) | — | —
addTempNodes/cleanTempNodes | OG h:324-325 | VEN same | NONE | ABSENT in TGT | PortalNode lifetime | OG ownership contract: getRoute attaches PortalNodes to the returned route; getFullPath calls cleanTempNodes after buildPath. OG BUG inherited by VEN: in getRoute(pos,pos), a non-empty route that then fails begin-leg validation is dropped on `continue` without cleanTempNodes → PortalNode leak per failed combo. TGT sidesteps by removing the feature entirely (header doc h:80: "hearthstones and mage teleporting was removed — route mutations requiring locking"). For the full-fidelity port: reintroduce PortalNode+tempNodes WITH a fixed lifetime (e.g. unique_ptr in route) — do not copy the OG leak.
getTotalDistance | OG cpp:1218 | VEN cpp:1225 | NONE | DIVERGED-fix (TGT cpp:1142): OG loops `i < size()-2` — skips the last leg AND for a 2-node route returns 0 (making isUselessLink's `nearLength + 0 < farLength*1.1` massively over-prune); size()==1 would underflow to a huge bound (OOB read). TGT: guard size<2→0, loop `i < size()-1`. KEEP TGT fix; do not port OG's bug back | linkDistanceTo | —
buildPath | OG cpp:1229 | VEN cpp:1236 | NONE | Both present in TGT: lowercase buildPath cpp:1295 = the cmangos port (faithful leg emission: AT/staticPortal 2-point entry+exit with pathObject, transport/flight FULL stored path via addPath, teleportSpell 2-point, walk with pop_back-if-not-last + skip-AT/transport-first-point trims, NODE_PREPATH head, NODE_PATH tail) with 2 deliberate deltas: (a) runtime link rebuild gated on non-null mover (both prevNode->BuildPath and reverse-temp variants) because resolvers hold only a shared lock — OG rebuilds unconditionally under shared lock (data race); with nullptr, TGT adds a null-mover LEG REPAIR: paths the missing walk leg into a LOCAL TravelNodePath via `prevPos.getPathTo(nodePos, nullptr)` accepted at explicit 2.0f tolerance (documented: default 0.1f targetPosRecalcDistance never satisfiable) — no shared mutation; (b) enum mapping areaTrigger→portal documented at cpp:1284-1294. Uppercase BuildPath cpp:1154 = modpb-evolved variant (transport/flight legs emit only departure+arrival points, not the stored path) slated for Phase-C removal | prevNode/isTransport/isPortal, TravelNode::BuildPath, PathNodeType | Assessment: keep (a) — it is the thread-safety correction plus a fidelity-improving repair; OG behavior (mutating rebuild) must NOT be restored under shared lock. If rebuilds are wanted, do them at generation time under unique lock.
TravelNodeRoute::print | OG cpp:1329 | VEN cpp:1336 | NONE | PORTED-FAITHFUL (TGT cpp:1443) | getDisplayX/Y | —

TravelNodeStub | OG h:337-347 | VEN same | NONE | PORTED-FAITHFUL renamed (TGT h:578): m_f/m_g/m_h→totalCost/costFromStart/heuristic, close→closed; OG oddity `uint32 currentGold = 0.0` cleaned | — | —

================================================================
TravelNodeMap — routing
================================================================

getRoute(TravelNode*,TravelNode*,Unit*) / GetNodeRoute | OG cpp:1497 | VEN cpp:1504 | ADAPTATION: FindPlayer, GetSpellTargetPosition(EFFECT_0); otherwise NONE | DIVERGED (TGT GetNodeRoute cpp:1588, Player* param): FAITHFUL — start==goal reject, hasRouteTo gate, gold seeding (HasCheat(gold)→10000000 else free-money value; non-bot → GetMoney), link getCost(bot,gold)/getPrice gold decay for non-taxi-cheaters, g/h/f relax with open/closed skip, parent chain unwind+reverse. MISSING vs OG: (a) group-aware gold — OG takes min "free money for" across group members (leader-gated, IsSafe, via sObjectMgr.GetPlayer) — TGT uses only the bot's own; (b) hearthstone PortalNode injection ("action useful hearthstone" + home-bind node ≤50y, g=max(2,(10−deathCount)·MINUTE)); (c) mage teleport spells {3561,3562,3563,3565,3566,3567,18960} PortalNode injection (HasSpell, IsSpellReady, has-reagents, target-pos node ≤10y, g=MINUTE); (d) FLT_MAX distance skip (OG cpp:1688) — TGT computes h unconditionally (fDist cross-map returns mapoffset-based value in both; OG's FLT_MAX guard covers unoffset maps). LOCAL CHANGES (flagged in prompt): lazy-deletion duplicate-push min-heap + closed-skip + MAX_A_STAR_EXPLORED=3000 runaway guard. Assessment: KEEP the lazy-deletion heap — OG's loop does a FULL std::sort of `open` every iteration (its heap ops are decorative and its comparator would build a max-heap; correctness came only from the per-iteration sort, at O(n²logn)); TGT is the same search done right. Graft (a)-(c) back onto TGT's loop when values ("free money for", "action useful", "death count", home-bind, reagents) and PortalNode land; (d) is a one-line re-add | BudgetValues NeedMoneyFor::travel, BotCheatMask, isTaxiCheater, links map | OG dead code: second `open.size()==0 && !hasRouteTo` check at cpp:1628 (already gated) — don't port.

getRoute(WorldPosition,…) | OG cpp:1717 | VEN cpp:1724 | ADAPTATION: m_mapId; otherwise NONE (keeps hearthstone last-resort + deadzone logging) | DIVERGED (TGT cpp:1822) — faithful core: transport-entry start filtering, closest-5 candidates both ends (OG partial_sort+resize; TGT resize of already-sorted getNodes — equivalent), per-endNode tail computed once (live navmesh, 1.0f accept, water-surface retry both ends), begin leg = cropPathTo(caller probe, maxStartDistance 20 transport/1 else) → live getPathTo → water-surface retry, badStartNodes memo. Deltas: (1) transport early-return placement — OG returns the FIRST combo's route even if EMPTY (quirk: on-deck bots try exactly one pair); TGT continues past empty routes and returns the first non-empty with cleared legs — improvement, keep; (2) TGT-only self-node rotation + single-node routes (`TravelNodeRoute({startNode})`) for cave-leaf nodes; (3) TGT-only hasRouteTo pre-gate + per-stage fail counters (pairsNoReach/noAstar/tailFails/beginFails) + thread_local s_lastRouteFailReason + GetLastRouteFailReason (cpp:1817) — diagnostics only, KEEP; (4) TGT-only node-approach ("trust-the-nodes") fallback (cpp:2002-2055): when no validated tail exists, ride the graph to the node nearest the destination (empty tail, must beat probe endpoint by >10y, >20y from node) and re-resolve from there — NO OG equivalent; OG compensates with denser graph (helper/dock/manual nodes). Assessment: KEEP until generation parity (helper+dock+manual+portal nodes) is ported, then re-evaluate; (5) MISSING OG hearthstone last-resort (temp botNode + route from bot position over endNodes, cpp:1856-1882) — depends on PortalNode/tempNodes machinery; port together with A* items (b)/(c); (6) MISSING deadzone.csv testPathToLoop instrumentation (testPathToLoop ABSENT in TGT) | cropPathTo, isPathTo, setAtWaterSurface, GetTransport | Callers hold m_nMapMtx shared (TGT doc h:708).

FindRouteNearestNodes | ABSENT | ABSENT | — | TGT-only (cpp:1720) | PathGenerator direct, nth_element K=3 | modpb-evolved resolver used by TravelPlan flow; Phase-C removal candidate once cmangos flow is sole path.

getFullPath | OG cpp:1887 (static) | VEN cpp:1894 | ADAPTATION(marked)×2 + FIX: (1) `reachedByNavmesh` temp var (cosmetic); (2) FIX leaked shared lock on the empty-route early return — OG cpp:1906-1910 returns WITHOUT unlock_shared → lock starvation (real OG bug); (3) marked fallback: when route empty but beginPath.size()>1, return TravelPath(beginPath) (partial probe walk) — deliberate deviation for the fragmented port graph | DIVERGED (TGT cpp:2119, non-static): faithful skeleton (probe → spell-distance shortcut → shared lock → getRoute → buildPath → unlock) with deliberate deltas, each documented: probe is flat-only (`allowSteepFallback=false` — new getPathFromPath param); accept tightened spellDistance(28.5y)→10.0f AND requires ≥… (probe false-reach fix); mirrors VEN's partial-probe fallback (+LOG_DEBUG); buildPath called with nullptr mover (thread safety, see above); fail-reason diagnostics; no cleanTempNodes (no temp nodes) | m_nMapMtx, getPathFromPath(40 iters), spellDistance | Assessment: KEEP lock fix (mandatory), KEEP nullptr-mover; flat-probe + 10y accept are behavior changes vs OG — retain while graph is sparse, revisit at graph parity. VEN retains OG's spellDistance accept + steep probe.

GetFullPath(TravelPlan&…) | ABSENT | ABSENT | — | TGT-only (cpp:2066) | TravelPlan struct (h:593), getRoute, BuildPath | modpb TravelPlan executor entry (MovementActions.cpp:3383). Phase-C removal candidate; keep listed as coexisting.

testPathToLoop | OG cpp:3654 (+PRINT_PATH macro 3643) | VEN cpp:3668 | NONE | ABSENT in TGT | PathFinder direct, NAV_AREA_WATER area costs (10/5/20), LastMovementValue, "deadzone.csv" | Diagnostics harness for getRoute; port only if deadzone logging wanted.

================================================================
TravelNodeMap — node container/lookup
================================================================

TravelNodeMap(TravelNodeMap* baseMap) copy | OG cpp:1346 (h:354) | VEN same | NONE | ABSENT in TGT (singleton, copies deleted h:795-799) | shared lock on base | OG used for offline graph copies; not needed at runtime.
instance/sTravelNodeMap | OG h:452 MaNGOS::Singleton | VEN same (shimmed) | NONE | PORTED (TGT h:629/825 Meyers singleton) | — | —
addNode | OG cpp:1372 | VEN cpp:1379 | NONE | DIVERGED-minor (TGT cpp:1462): m_map_nodes per-map index REMOVED (single `nodes` vector); name-dedup loop scans ALL nodes (OG scans same-map via getNodes(pos)); transport/transportId params ignored ([[maybe_unused]]) — OG also ignores them (commented-out members) | getNode(pos,5.0f) dedup, std::regex | Per-map index loss = O(N) scans in getNodes; TGT compensates with m_mapIndex/m_zoneIndex built at Init for the evolved paths, but cmangos-path getNodes still scans all ~4-5k nodes per call under shared lock. Port consideration: restore m_map_nodes or route getNodes through m_mapIndex.
removeNode | OG cpp:1409 | VEN cpp:1416 | NONE | PORTED-FAITHFUL minus map index (TGT cpp:1500) | removeLinkTo(nullptr,true) | Runtime-unused in TGT (immutable graph).
removeNodes | OG h:358 inline | VEN same | NONE | PORTED-FAITHFUL (TGT h:643, try_lock_for 10s) | m_nMapMtx | —
fullLinkNode | OG cpp:1436 | VEN cpp:1443 | NONE | PORTED-FAITHFUL (TGT cpp:1516) | BuildPath both dirs | Generation-only.
getNodes() | OG h:362 | VEN same | NONE | PORTED-FAITHFUL (TGT h:660, returns copy) | — | —
getNodes(pos,range,transportEntry) | OG cpp:1456 | VEN cpp:1463 | NONE | DIVERGED-minor (TGT cpp:1536): full-vector scan + map filter instead of m_map_nodes bucket; sort identical; transportEntry filter identical | getTransportId | Perf note above.
getNode(sameNode) | OG h:366 (per-map bucket) | VEN same | NONE | DIVERGED-trivial (TGT h:666, scans all) | — | —
getNode(pos,ppath,bot,range) | OG cpp:1475 | VEN cpp:1482 | NONE | PORTED-FAITHFUL (TGT cpp:1561; `bot->IsInWorld()`→`bot->GetMap()` guard; ppath now [[maybe_unused]] — OG also never used it) | canPathTo, max-5 attempts | —
getNode(pos,bot,range) wrapper | OG h:368 | VEN same | NONE | PORTED-FAITHFUL (TGT h:681) | — | —
getRandomNode | OG h:371 | VEN same | NONE | PORTED-FAITHFUL (TGT h:689) | urand | —
manageNodes | OG cpp:2004 | VEN cpp:2027 | NONE | ABSENT in TGT — deliberate (header doc h:53-55: runtime mutation removed; unique_lock caused 100-250ms contention spikes) | try_lock, cropUselessNode/addZoneLinkNode/addRandomExtNode, printMap | For full fidelity this is a POLICY divergence, not an omission bug; restoring it re-opens the lock-contention problem. Recommend: keep removed at runtime; expose equivalents via console generation commands only.
cropUselessNode | OG cpp:1921 | VEN cpp:1944 | NONE | PORTED-FAITHFUL (TGT cpp:2205) | getNodeMap(true, ignore) | Generation/console only in TGT.
addZoneLinkNode | OG cpp:1942 | VEN cpp:1965 | NONE | PORTED-FAITHFUL (TGT cpp:2226) | getAreaName transitions | —
addRandomExtNode | OG cpp:1970 | VEN cpp:1993 | NONE | PORTED-FAITHFUL (TGT cpp:2252) | urand path sampling | —
setHasToGen/gethasToGen | OG h:385-386 | VEN same | NONE | DIVERGED-trivial (TGT h:724 setHasToGen() no param; gethasToGen ABSENT) | — | —
LoadMaps | OG cpp:2064 | VEN cpp:2087 | ADAPTATION(marked): whole tile pre-load no-oped — "AC's PathGenerator loads tiles on demand, nothing to pre-load" (VEN keeps dead mmtile-scan block referencing MMapFactory shim) | ABSENT in TGT | MMapFactory (cmangos) | AC loads mmap tiles lazily via MMapMgr; genuinely not needed — but node GENERATION quality depends on tiles being loadable for all maps at gen time (TGT relies on on-demand load inside PathGenerator; adequate).

================================================================
TravelNodeMap — generation chain
================================================================

generateNpcNodes | OG cpp:2117 | VEN cpp:2131 | ADAPTATION: field casing only | PORTED-FAITHFUL (TGT cpp:2286) | getCreaturesNear, npcflag mask, rank 3 / rank 1 non-overworld boss dedup | —
generateStartNodes | OG cpp:2179 | VEN cpp:2193 | NONE | DIVERGED-minor (TGT cpp:2348): startNames table missing RACE_BLOODELF/RACE_DRAENEI (OG has them under !MANGOSBOT_ZERO; WotLK core creates their nodes with empty prefix "…start") and goblin | GetPlayerInfo | Cosmetic (node names) — fix in port.
generateAreaTriggerNodes | OG cpp:2216 | VEN cpp:2230 | ADAPTATION: GetAreaTriggerTeleport + m_mapId | DIVERGED-minor (TGT cpp:2380): iterates GetAllAreaTriggerTeleports (equivalent coverage); does NOT apply OG's `z - 4.0f` entry-point drop (AT floats above ground → node may sit 4y high, affecting navmesh snap of begin/tail legs); link extraCost 3.0f + type 2 identical; pathObject = trigger id | sObjectMgr AT stores | Port the −4.0f for fidelity.
generatePortalNodes | OG cpp:2285 | VEN cpp:2299 | ADAPTATION: GetSpellTargetPosition(EFFECT_0); sBotLog | ABSENT in TGT | GOStorage SPELLCASTER, SPELL_EFFECT_TELEPORT_UNITS, SpellTargetPosition, height clamp | Static portals (Dalaran crater→IF etc.) missing from TGT graph → staticPortal edges only via DB. Port for parity.
makeDockNode | OG cpp:2358 | VEN cpp:2372 | NONE | ABSENT in TGT | ClosestCorrectPoint(20,1), loadMapAndVMap | Dock/exit helper nodes for transports; without it transport nodes hang in air/water with no walk-on link. Port with transports.
generateTransportNodes | OG cpp:2382 | VEN cpp:2396 | ADAPTATION: sTransportMgr-> | DIVERGED (TGT cpp:2447): boats/zeppelins-with-taxi-path only. MISSING: displayId 808 plunger skip; ENTIRE elevator/tram branch (TransportAnimation path rotation, dual-pass stop detection, per-display Z exit offsets 3831/807/455); makeDockNode calls (boat +6.0, zeppelin −17.0, moonspray +4.88 exit offsets); OG's second pass exists in TGT (wrap-around leg) | sTaxiPathNodesByPath, moTransport | Largest generation gap: TGT has no docks and no elevators. Cross-refs memory note "transport navmesh gap".
generateZoneMeanNodes | OG cpp:2604 | VEN cpp:2618 | NONE | PORTED-FAITHFUL modulo TravelMgr container shape (TGT cpp:2524: exploreLocs loc.second->getPoints(true)) | TravelMgr explore locs, WP_MEAN_CENTROID | —
addManualNodes | OG cpp:2626 | VEN cpp:2640 | NONE | ABSENT in TGT | hardcoded WorldPositions (Sunken Temple, Deadmines exit, Tauren start, Maraudon, BFD, Dire Maul + link, WC, Timbermaw; MANGOSBOT_TWO: Blackrock, Ebon Hold AT-link, Scourge-gryphon flightPath links 29488/29501, Noxious Pass) | Cave/instance connectivity relies on these; TGT's node-approach fallback partially compensates. Port (WotLK subset incl. MANGOSBOT_TWO block).
generateNodes | OG cpp:2709 | VEN cpp:2723 | NONE | DIVERGED (TGT cpp:2544): calls only start/npc/AT/transport/zone-mean — omits generatePortalNodes + addManualNodes (absent) | — | —
generateWalkPathMap | OG cpp:2727 | VEN cpp:2741 | NONE | ABSENT as separate fn; inlined in TGT generateWalkPaths | BarGoLink, m_nMapMtx as bar lock | —
generateWalkPaths | OG cpp:2762 (async per map via std::async) | VEN cpp:2776 | NONE | DIVERGED (TGT cpp:2558): single-threaded per-map loop; drops OG's `endNode->isTransport() && endNode->isLinked()` skip; same 2000y radius, isLinked skip, hasCompletePathTo skip, same-map gate, BuildPath(nullptr) | BuildPath | Missing transport-endNode skip re-builds paths INTO transport nodes each gen (wasteful, can create odd walk links onto decks) — re-add.
generateHelperNodes (both) | OG cpp:2795/2895 | VEN cpp:2809/2909 | NONE | ABSENT in TGT | getRouteSize, canPathTo, per-map async | Places nodes to connect unreachable spots (caves, tauren start). Its absence is why TGT needed the node-approach fallback. Port for parity (needs getRouteSize + mapOnly getNodeMap).
removeLowNodes | OG cpp:3015 | VEN cpp:3029 | NONE | PORTED-FAITHFUL (TGT cpp:2652, minus log) | getNodeMap(true) | <5-component overworld purge.
removeUselessPathMap | OG cpp:3044 | VEN cpp:3058 | NONE | ABSENT as separate fn; inlined in TGT removeUselessPaths | — | —
removeUselessPaths | OG cpp:3080 (async per map) | VEN cpp:3094 | NONE | DIVERGED (TGT cpp:2679): single-threaded, global not per-map; MISSING OG's per-iteration `startNode->clearRoutes()` sweep — after cropping, stale `routes` caches poison isUselessLink's route probes in later iterations (OG resets each pass). Also inherits the vestigial `MANGOS_ASSERT(true)`/`ASSERT(true)` no-op loop from OG (dead in all three) | cropUselessLinks, hasToSave | Re-add clearRoutes per iteration.
calculatePathCosts | OG cpp:3110 (async futures, repeats until none) | VEN cpp:3124 | NONE | PORTED-FAITHFUL-simplified (TGT cpp:2709, sync single pass — outcome equivalent since calculateCost is idempotent) | calculateCost | —
generateTaxiPaths | OG cpp:2962 | VEN cpp:2976 | NONE | DIVERGED-minor (TGT cpp:2598): MISSING OG endpoint fix-ups (prepend startNode pos / append endNode pos when >0.1f from taxi path ends — without them flight legs start/stop mid-air relative to node); ADDED walk-path-preserve guard (skip setPathTo if an existing walk path would be overwritten — fixes FM-position resolving to an innkeeper node and destroying its walk link; not in OG, KEEP); speed formula 450*8 identical | sTaxiPathStore/NodesStore/PathNodesByPath, getPathLength | Port endpoint fix-ups, keep guard.
generatePaths | OG cpp:3147 | VEN cpp:3161 | NONE | DIVERGED (TGT cpp:2730): OG order = SetMobAvoidArea → walk → helpers(opt) → removeLow (ALWAYS) → removeUseless (ALWAYS) → printMap → costs → taxi; TGT = walk → [fullGen: removeLow+removeUseless] → costs → taxi. Missing SetMobAvoidArea (TravelMgr), helpers, always-prune, printMap | — | Align during generation-parity port.
generateAll | OG cpp:3173 | VEN cpp:3187 | NONE | DIVERGED (TGT cpp:2750): OG = LoadMaps(cond) → generateNodes(cond) → calcMapOffset → LoadMapTransfers → generatePaths(cond) → coverage warm (`node->hasRouteTo(node)` for all — pre-crash-proofing); TGT = generatePaths(false) → saveNodeStore → BuildZoneIndex → PrecomputeReachability (console cmd path). TGT Init (cpp:2760, TGT-only, called from TravelMgr::Init cpp:4486) = InitTaxiGraph → LoadNodeStore → calcMapOffset → conditional gen → BuildZoneIndex → PrecomputeReachability | — | PrecomputeReachability replaces OG's coverage warm.

================================================================
TravelNodeMap — store, offsets, indexes, locks
================================================================

printMap | OG cpp:3200 | VEN cpp:3214 | NONE | PORTED-FAITHFUL (TGT cpp:2782; passes print(false) vs OG print(hasToSave)) | travelNodes/travelPaths.csv | —
printNodeStore | OG cpp:3221 | VEN cpp:3235 | NONE | PORTED-FAITHFUL (TGT cpp:2803; drops `namespace ai` wrapper) | TravelNodeStore.h log | —
saveNodeStore | OG cpp:3305 (force param) | VEN cpp:3319 | ADAPTATION(marked): transactions no-oped ("bot SQL runs standalone; transaction model deferred"), BotPExecute shims | DIVERGED (TGT cpp:2891, no force param): tables `playerbots_travelnode*` (OG `ai_playerbot_travelnode*`); prepared statements + chunked transactions (500 stmts; points: multi-row INSERT 500/row-batch, 10k rows/tx — documented fix for the ~1.5M-point mega-transaction that partial-committed and corrupted the DB); MISSING OG's deterministic ordering (sort nodes and links by name+map+x before id assignment → stable ids across saves; TGT saves in insertion order); async-queue flush warning added | PlayerbotsDatabase, PLAYERBOTS_{DEL,INS}_TRAVELNODE* | Keep TGT chunking; consider re-adding deterministic sort (diff-able DB dumps).
loadNodeStore / LoadNodeStore | OG cpp:3410 | VEN cpp:3424 | ADAPTATION: BotPQuery + Field Get<> API | DIVERGED (TGT cpp:3071): prepared statements; addNode called with checkDuplicate=false (OG default true → 5y-merge of duplicate DB rows; TGT trusts ids — duplicates would double-load); missing-id guards added (OG `saveNodes.find(...)->second` on absent id is UB — TGT fixed, KEEP); missing-nodes-table early return dropped (harmless, guards make later loops no-op); `calculated→complete` promotion identical; MISSING both OG data hotfixes: (1) reverse inverted transport paths (front==endNode && back==node → reverse), (2) taxi paths not starting at node (prepend/append node positions) — needed when consuming OG-era DB dumps; irrelevant for freshly generated TGT data | PLAYERBOTS_SEL_TRAVELNODE* | Decide by data provenance: if importing cmangos-generated node dumps, port both hotfixes.
calcMapOffset | OG cpp:3578 | VEN cpp:3592 | NONE | DIVERGED-fix (TGT cpp:3187): OG BUG — index `i` never incremented in the offset-assignment loop, so every non-overworld map gets offsets computed from min[0]/max[0]; VEN inherits; TGT adds `i++` (cpp:3252). KEEP TGT | isOverworld, fixed offsets for maps 0/1/530/571 | fDist correctness across instanced maps depends on this — OG bug skews A* heuristics for instance nodes.
getMapOffset | OG cpp:3794 | VEN cpp:3808 | NONE | PORTED-FAITHFUL (TGT cpp:3256) | — | —
m_nMapMtx discipline | OG h:439 | VEN same | VEN adds the getFullPath unlock fix (above) | TGT h:789 + doc h:82-87: graph immutable post-Init; shared_locks in GetFullPath (scoped guard, cpp:2097) and getFullPath (manual lock/unlock pair, cpp:2153/2174/2200 — all paths covered); exclusive only in DebugAction dumps (DebugAction.cpp:203/230) and removeNodes; saveNodeStore itself takes NO lock (callers: TravelMgr.cpp:3038 at startup, DebugAction.cpp:272) | — | OG discipline was messier: manageNodes try_lock writes at runtime; generateWalkPathMap abuses the mutex as a BarGoLink lock; getFullPath leaked shared on early return (fixed in VEN/TGT). TGT policy (immutable + shared-only) is sound; the cmangos-port getFullPath still uses manual lock/unlock — converting to scoped guard is a safe cleanup.
InitTaxiGraph/FindTaxiPath/BuildTaxiGraph/ComputeAllPaths/BFS/BuildPath(taxi) | ABSENT | ABSENT | — | TGT-only (cpp:3268-3386) | sTaxiPathStore | modpb flight-dispatch support (multi-hop taxi node sequences). No OG counterpart (OG encodes single taxi edges into the node graph); keep — the cmangos flightpath legs and this coexist.
BuildZoneIndex/GetNearestNodeInZone/GetNodesInZone/GetNearestNodeOnMap | ABSENT | ABSENT | — | TGT-only (cpp:3388-3467) | sMapMgr GetZoneId | Zone-indexed nearest-node lookups for the evolved resolver; keep.
PrecomputeReachability | ABSENT (OG: lazy per-node directed BFS in hasRouteTo) | ABSENT | — | TGT-only (cpp:3469) — UNDIRECTED connected components prefill `routes` for O(1) hasRouteTo | reverse-link closure, setRouteTo | Assessment: KEEP. Rationale documented in-code: directed one-global-visited BFS strands nodes behind one-way edges → false negatives → fallback to raw probes; undirected closure has only false positives which the A* (ground truth) filters at bounded cost (MAX_A_STAR_EXPLORED). OG's per-node lazy directed BFS is CORRECT per node (no false results) but O(V·E) worst-case at runtime; if strict OG fidelity is demanded, the faithful equivalent is per-node lazy getNodeMap — measurably worse and reintroduces the fragmented-graph freeze. Caveat: after any generation-time link cropping, reachability must be recomputed (Init order does this; removeUselessPaths internally still needs the clearRoutes fix noted above).

MAX_PATHFINDING_DISTANCE (296.0f) | ABSENT as constant (OG doc-comment only) | ABSENT | — | TGT-only h:90 | — | —
TravelPlan struct | ABSENT | ABSENT | — | TGT-only h:593 | G3D::Vector3 | modpb executor state; Phase-C removal target along with GetFullPath/BuildPath(capital)/FindRouteNearestNodes.

================================================================
DEPENDENCY SUMMARY — what Scope B needs from other subsystems
================================================================

From VALUES (Scope: values/triggers):
- "possible attack targets" (list<ObjectGuid>, AI_VALUE_LAZY) and "hazards" (list<HazardPosition>, HazardsValue.h) — hard blockers for ClipPath enemy/hazard scans (explicit PORT-TODO at TGT cpp:1092).
- "free money for" (BudgetValues, NeedMoneyFor::travel) — present; "group members", "action useful"(hearthstone), "death count", "has reagents for", "home bind" — needed to restore OG A* gold-across-group + hearthstone/teleport PortalNode injection.
- LastMovementValue ("last movement") — only for testPathToLoop diagnostics.

From WorldPosition (Scope: WorldPosition/TravelMgr): sqDistance, distance, fDist (mapoffset-aware — depends on calcMapOffset fix), projectOnSegment, getPathTo, getPathFromPath (incl. TGT's new allowSteepFallback param), isPathTo, cropPathTo, canPathTo, closestSq (for full isUselessLink), setAtWaterSurface, getCreaturesNear, isInWater/isVmapLoaded, getAreaName, isOverworld/isUnderWater, currentHeight, ClosestCorrectPoint + loadMapAndVMap (makeDockNode), getPathLength, printWKT, getVector3/fromPointsArray.

From PlayerbotAI/infra: IsStateActive/BotState (ClipPath), HasCheat/BotCheatMask::gold, GET_PLAYERBOT_AI, isTaxiCheater, ServerFacade::IsSpellReady, CanSpellClick (getCost spell-click flightpaths), GetFactionReaction/friendTo* equivalents.

From config (sPlayerbotAIConfig keys used in this scope): tooCloseDistance, reactDistance, spellDistance, targetPosRecalcDistance, transportTeleportType, hasLog/log/openLog ("crop.csv", "deadzone.csv", "travelNodes.csv", "travelPaths.csv", "TravelNodeStore.h"), GetTimestampStr.

From core/DB: sObjectMgr AreaTrigger + AreaTriggerTeleport stores, SpellMgr::GetSpellTargetPosition(spellId, EFFECT_0), sTaxiPathStore/sTaxiNodesStore/sTaxiPathNodesByPath, TransportMgr::GetTransportAnimInfo (elevators — unported), GameObjectTemplate(+Addon faction), PlayerbotsDatabase prepared statements PLAYERBOTS_{SEL,INS,DEL}_TRAVELNODE{,_LINK,_PATH} (tables playerbots_travelnode*).

From TravelMgr: exploreLocs (zone means), SetMobAvoidArea (OG generatePaths — absent in TGT), LoadMapTransfers (OG generateAll), TravelMgr::Init → TravelNodeMap::Init ordering.

From MovementActions (Scope A/C consumers, verified live): MoveTo2 chain calls makeShortCut/UpcommingSpecialMovement/ClipPath (MovementActions.cpp:4126/4135/4146), getFullPath (:3672), modpb GetFullPath (:3383), UpcommingSpecialMovement again at :3561.

TOP PORT-CRITICAL FINDINGS (ranked): (1) ClipPath enemy+hazard scans stubbed — needs values port; (2) getCost missing Outland/Northrend level gates + staticPortal faction gate + spell-click flightpath fallback; (3) generation parity gaps — dock nodes/elevators/portal nodes/manual nodes/helper nodes all absent (root cause the node-approach fallback papers over); (4) cropUselessLinks one-direction removal + removeUselessPaths missing clearRoutes + isUselessLink missing AT logic (generation pruning fidelity); (5) BuildPath reverse-mirror block creates unvalidated reverse links (replace with OG one-way semantics); (6) OG bugs that must NOT be ported back: getFullPath shared-lock leak, ClipPath cut-before-end-check, getTotalDistance size-2/underflow, calcMapOffset missing i++, PortalNode leak on failed-validation combos, A* sort-every-iteration heap.
