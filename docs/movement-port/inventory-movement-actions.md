All sources read in full. Compiling the audit.

# SCOPE A AUDIT — MovementAction & movement-executing actions

Path aliases:
- **OG** = `C:/Users/Admin/git/scratch/cmangos-playerbots/playerbot/strategy/actions/MovementActions.cpp` (header `.h`)
- **V** = `C:/Users/Admin/git/azerothcore-wotlk/modules/mod-cmangosbots/src/playerbot/strategy/actions/MovementActions.cpp`
- **T** = `C:/Users/Admin/git/main/azerothcore-wotlk/modules/mod-playerbots/src/Ai/Base/Actions/MovementActions.cpp` (branch feature/new_rpg_and_nav_5_bash)
- **OG-TT/V-TT** = `.../MoveToTravelTargetAction.cpp`; **OG-RT/V-RT** = `.../MoveToRpgTargetAction.cpp`; **NRB** = `C:/Users/Admin/git/main/azerothcore-wotlk/modules/mod-playerbots/src/Ai/World/Rpg/Action/NewRpgBaseAction.cpp`

Headers OG `.h` vs V `.h` are 1:1 identical except `GenericTransport*`→`Transport*` (line 21) and `WorldLocation` field names (line 30) — both API-driven. Vendored build defines `MANGOSBOT_TWO` + `CMANGOS` via `src/botcompat/botcompat.h:27`.

## A. MovementAction core — A→B pipeline

**MoveTo2(WorldPosition,...)** | OG :1063 | V :1103 | delta: NONE except AC API renames (`InterruptMoving`→`StopMoving` V:1145, `HandleEmoteState(0)`→`ClearEmoteState`, `MOVEFLAG_WALK_MODE`→`MOVEMENTFLAG_WALKING`, `IsNonMeleeSpellCasted`→`IsNonMeleeSpellCast`) and dropped the OG "[BOT PATH BUG]" Ironforge-AH debug block (OG :1242-1303, local diagnostic — unmarked but debug-only) | TARGET: PORTED-DIVERGED at T:4017 (+coord overload T:4226). Faithful skeleton (nextTeleport wait, WaitForTransport, targetPosRecalcDistance stop, FlyDirect hook, ResolveMovePath, makeShortCut, UpcommingSpecialMovement→HandleSpecialMovement, transport bail, ClipPath, WaitForReach, stand/interrupt, far-teleport, masterWalking, generatePath calc, DispatchMovement, ClearIdleState). Divergences: (1) added **unstick-step recovery** T:4077-4123 on empty path (ClosestCorrectPoint(10,10) + LOS + 2-15y gates, raw MovePoint); (2) far-teleport goes through **SafeBotTeleport** (T:60, Z-validation, returns false on unresolved Z) instead of raw TeleportTo; (3) `lastMove.moveEvent = ai->GetLastEvent()` DROPPED — target `LastMovement` has **no moveEvent field** (LastMovementValue.h); (4) underwater→`setAtWaterSurface()` waypoint snap is a PORT-TODO comment (T:4151), helper absent; (5) debug-move waypoint spawning block (OG :1154-1163) absent (replaced by EmitDebugMove); (6) `SetDuration`→`botAI->SetNextCheckDelay`; (7) `IsUnderwater(bot)` term dropped from generatePath calc (T:4202, AC IsInWater covers) | DEPS: LastMovement (lastPath/nextTeleport/lastTransportEntry), TravelPath (makeShortCut/UpcommingSpecialMovement/ClipPath/getPointPath/getBack), WorldPosition (distance/getPathLength/isInWater/isUnderWater/IsValid/getAngleTo), sTravelNodeMap, AllowActivity(DETAILED_MOVE_ACTIVITY), HasPlayerNearby, config: targetPosRecalcDistance, reactDistance, walkDistance | NOTES: OG MoveTo2 never updates `lastMoveShort` (only the dead legacy MoveTo body did at OG :2176); target's native MoveTo priority system is bypassed entirely by MoveTo2 — MoveTo2 doesn't set LastMovement::Set(...)/priority, so `IsWaitingForLastMove` gates don't see MoveTo2 movement.

**ResolveMovePath** | OG :681 | V :711 | delta: ADAPTATION(marked V:749-757) — removed OG's `if (outMovePath.empty()) outMovePath.addPoint(endPosition)` fabricated 1-point path (frozen-bot fix); rest identical (`InBattleGround`→`InBattleground`) | TARGET: PORTED-DIVERGED T:3626. Keeps vendored no-fabrication policy; adds: (1) **TRAVELNODE_THRESHOLD = 50.0f hardcoded** replacing `sPlayerbotAIConfig.sightDistance` gate (deliberate, commented T:3650-3656); (2) map-609 special case now unconditional (OG/V had it `#ifdef MANGOSBOT_TWO`) — correct for WotLK; (3) **no-progress collapse** T:3719-3725 (paths <2 points or ending <1y from start with total >2y → cleared); (4) EmitDebugMove diagnostics incl. `TravelNodeMap::GetLastRouteFailReason()` (target-only API) | DEPS: sTravelNodeMap.getFullPath/getNodes, WorldPosition::getPathTo, LastMovement.lastPath | NOTES: last-path-reuse test (`getBack().distance(endPos) < maxDistChange`) and worse-path-keep test (`lastPath.getBack() <= outMovePath.getBack()`) preserved verbatim in all three.

**DispatchMovement** | OG :957 | V :992 | delta: ADAPTATION(marked V:1032-1038) — `usePath` flipped false→true so the precomputed path is played via `MoveSplinePath` (AC EscortMovementGenerator) instead of `MovePoint(back, generatePath)` which re-ran PathGenerator and froze bots; also FORCED_MOVEMENT_FLIGHT→RUN (marked, AC lacks FLIGHT); `MovePath`→`MoveSplinePath` | TARGET: PORTED-DIVERGED T:3893. Keeps usePath=true architecture; adds: (1) empty-path guard T:3900; (2) **bounded hop** in the !generatePath/MovePoint branch T:3910-3940 (furthest waypoint within reactDistance instead of path.back(); WaitForReach on hop only); (3) **MAX_SPLINE_SEGMENT = 50y truncation** T:3965-3984 (starting at i=2, never first segment); (4) per-point ground-conform `UpdateAllowedPositionZ` + transport offset kept (T:3998-4005) | DEPS: MotionMaster::MoveSplinePath/MovePoint, TravelPath::getPointPath, WorldPosition::getPathLength/getVector3, GeneratePathAvoidingHazards, WaitForReach | NOTES: OG's dead `usePath=false` MovePoint branch passes `!bot->IsFlying()` as final MovePoint arg (OG :1034) — an OG bug (inverted vs the top branch :987); vendored copied it verbatim in its unused branch (V:1075); target dropped that branch entirely.

**HandleSpecialMovement** | OG :725 | V :760 | delta: NONE (API only: `GetObjectGuid`→`GetGUID`, `MAX_GAMEOBJECT_TYPE`→`go->GetGoType()` — equivalent since the GO checks its own type; `packet.release()` queue) | TARGET: PORTED-DIVERGED T:3742. Faithful structure (STATIC_PORTAL spellcaster check incl. trigger-spell chase; AREA_TRIGGER entry/teleport; on-transport NODE_TRANSPORT both orders; NODE_FLIGHTPATH→UseTaxi; NODE_TELEPORT hearthstone/spell). Divergences: (1) teleports via SafeBotTeleport; (2) NODE_TELEPORT spell branch **drops** `sServerFacade.IsSpellReady` + `AI_VALUE2(uint32, "has reagents for", entry)` + `AI_VALUE2(uint32, "current mount speed", "self target")` — replaced by `!bot->HasSpellCooldown(entry)` + `bot->IsMounted()` (no reagent gate: bot may burn a cast without reagents / fail silently); (3) mounted-height guard reimplemented via `GetMapWaterOrGroundLevel` instead of `WorldPosition::currentHeight()`; (4) null-check on SpellInfo added | DEPS: "nearest game objects" value, "action useful"("hearthstone"), "last area trigger", "last movement", ChatHelper::FormatWorldobject, DoSpecificAction("cast"/"hearthstone"), config transportTeleportType | NOTES: OG/V write `lastTransportEntry` into the LastMovement value keyed **"last movement"**; the AREA_TRIGGER entry write goes into **"last area trigger"** value's `.lastAreaTrigger` — preserved in target.

**GetMover** | OG :1042 | V :1084 | delta: ADAPTATION(marked V:1087) — AC Vehicle API (`GetVehicle`/`GetSeatForPassenger`/`CanControl`) replacing cmangos TransportInfo | TARGET: PORTED-FAITHFUL T:3610 (identical to vendored form, `botVehicle->GetBase()`) | DEPS: Vehicle, VehicleSeatEntry | NOTES: none.

**FlyDirect** | OG :88 | V :124 | delta: ADAPTATION(marked V:217) — the needLand TerrainInfo height/water check is stubbed to `needLand=false` ("deferred to Phase-8") → vendored bot **never auto-lands** in the close-distance branch; also FORCED_MOVEMENT_FLIGHT→RUN (marked), `mm.Clear(false,true)`→`Clear(false)` (unmarked — AC Clear lacks 2nd arg; loses "keep current generator" semantics), MOVEFLAG_LEVITATING→MOVEMENTFLAG_DISABLE_GRAVITY | TARGET: STUBBED T:3575 — returns false, PORT-TODO lists missing WorldPosition helpers (isOutside/canFly/currentHeight/getHeight/limit/IsInLineOfSight) | DEPS(OG): WorldPosition isOutside/canFly/currentHeight/limit/getHeight/getPathTo/IsInLineOfSight, LastMovement.setPath/nextTeleport/lastAreaTrigger, MoveDelay, AllowActivity, HasPlayerNearby, SMSG_SPLINE_MOVE_SET/UNSET_FLYING, config targetPosRecalcDistance/reactDistance | NOTES: OG writes `lastAreaTrigger = movePosition` (a WorldPosition into a field also used for AT entries — OG quirk); OG's teleport branch sets nextTeleport throttle.

**UpdateFlyingState** | OG :858 | V :893 | delta: NONE (AC terrain API: `Map::GetHeight/GetWaterOrGroundLevel/isInLineOfSight`; MANGOSBOT_TWO LOS uses AC LINEOFSIGHT_ALL_CHECKS) | TARGET: STUBBED T:3590 no-op (PORT-TODO; only reachable when IsFreeFlying) | DEPS: terrain height/ground, LOS+100z probe, movement-flag packets | NOTES: OG has the same logic duplicated inside dead MoveTo (:2085-2173); live copy is the extracted function.

**GeneratePathAvoidingHazards** | OG :3030 | V :2146 | delta: NONE (coord field names only) | TARGET: STUBBED T:3600 pass-through returning false (PORT-TODO — no "hazards" value in target context) | DEPS: `AI_VALUE(std::list<HazardPosition>, "hazards")` (HazardsValue.h), CalculatePerpendicularPoint, IsValidPosition, debug SummonCreature | NOTES: called by DispatchMovement (all three) and OG/V ChaseTo; max 20 inserted points; hazardRangeOffset = range*1.5.

**CalculatePerpendicularPoint** (free fn) | OG :2521 | V :1627 | delta: NONE | TARGET: ABSENT | DEPS: WorldPosition arithmetic (operator-, /, *, size) | NOTES: needed by hazard rerouting + ChaseTo hazard dodge.

**IsValidPosition** | OG :2995 | V :2111 | delta: NONE (`MaNGOS::IsValidMapCoord`→`Acore::IsValidMapCoord`) | TARGET: ABSENT | DEPS: WorldPosition::canPathTo/IsInLineOfSight, IsHazardNearPosition | NOTES: private helper, hazard cluster.

**IsHazardNearPosition** | OG :3004 | V :2120 | delta: NONE | TARGET: ABSENT | DEPS: "hazards" value | NOTES: also used by OG/V MoveTo dead code (:2062) and ChaseTo.

## B. Transport / taxi / minimal-move legs

**UseTaxi** | OG :253 | V :283 | delta: NONE (`OnTaxiFlightEject(true)`→`CleanupAfterTaxiFlight()`) | TARGET: PORTED-DIVERGED T:3390 — **HandleSpellClick fallback dropped** (OG handled Ebon Hold gryphon spell-click when no TaxiPathEntry; target returns false, PORT-TODO); cheat top-up + refund-on-fail preserved | DEPS: sTaxiPathStore, "nearest npcs" value, BotCheatMask::gold/taxi, ActivateTaxiPathTo, SendLearnNewTaxiNode | NOTES: DK-start taxi will not work in target until spell-click leg ported.

**MoveOnTransport** | OG :313 | V :343 | delta: NONE structurally; API: `movgen->Interrupt(*bot)`→`movgen->Pause(0)` (unmarked, semantics close), `SendHeartBeat`→`BotSendHeartBeat` shim, `MovePath`→`MoveSplinePath` | TARGET: STUBBED-PARTIAL T:3447 — **teleport path only**; walk-on branch (RandomPointOnTrans/getPathStepFrom/toPointsArray) deferred (PORT-TODO, transport-navmesh gap) | DEPS: WorldPosition::RandomPointOnTrans/getPathStepFrom/toPointsArray, Transport::AddPassenger, PlayerRelocation | NOTES: OG boards by relocation+AddPassenger when doTeleport; else spline-walks aboard.

**MoveOffTransport** | OG :393 | V :423 | delta: NONE (API only) | TARGET: PARTIAL T:3471 — teleport disembark via SafeBotTeleport; non-teleport branch does the NearTeleportTo local-coords fixup then returns false (spline walk-off deferred) | DEPS: Transport::RemovePassenger, getPathStepFrom, toPointsArray | NOTES: OG requires path.back() within √5y of exitPos.

**UseTransport** | OG :440 | V :470 | delta: NONE (`mapid`→`m_mapId`) | TARGET: PORTED-DIVERGED T:3495 — logic preserved (already-aboard→exit-if-docked/else 50-in-51 reshuffle; find nearest transport by entry; board if docked), all `TellDebug` narration dropped | DEPS: WorldPosition::getTransports(entry)/sqDistance2d/fDist, sGOStorage (OG name lookup dropped in target) | NOTES: `urand(0,50)` reshuffle chance preserved nowhere in target (walk-on stubbed makes it moot).

**WaitForTransport** | OG :650 | V :680 | delta: NONE | TARGET: PORTED-FAITHFUL T:3541 + added `lastMove.lastPath.empty()` guard (OG dereferences `.front()` on a possibly-empty path — **OG bug fixed silently in target, still present in vendored** V:690) | DEPS: LastMovement.lastTransportEntry/lastPath, TravelPath::UpcommingSpecialMovement | NOTES: vendored crash-candidate if lastTransportEntry set while lastPath empty.

**MinimalMove** | OG :505 | V :535 | delta: NONE except `GetSpeedInMotion()`→`GetSpeed(MOVE_RUN)` (unmarked, ignores mount/swim speed in ETA calc) and `IsTaxiFlying`→`IsInFlight`, yell-range config rename | TARGET: **ABSENT** (no caller; the passive/offline teleport-walk driver was not ported) | DEPS: LastMovement.lastPath/nextTeleport, PathNodePoint::isWalkable, UseTaxi/UseTransport, HasPlayerNearby(pos, yellRange), sPerformanceMonitor, config enableMinimalMove/passiveDelay | NOTES: this is the OG mechanism for far/inactive bots to progress along stored paths by teleport hops; target compensates with MoveTo2's far-teleport + travel-stuck teleports, but there is no path-following teleport walker.

## C. Movement primitives

**MoveTo(mapId,x,y,z,...)** | OG :1313 (immediately `return MoveTo2(...)`; :1317-2182 is **unreachable legacy body** retained in-file) | V :1290 (clean 3-line forward; dead body deleted) | delta: ADAPTATION (dead-code removal; per memory rule "keep dead ifdef branches" this was still removed — flag for port plan: OG dead body is the reference for lastMoveShort/`urand(0,1)` shortcut/aggro-clip variants) | TARGET: **DIVERGED — native rewrite** T:372, NOT a forward to MoveTo2. Target MoveTo is the short-range primitive: UpdateMovementState → IsMovingAllowed/IsDuplicateMove/IsWaitingForLastMove gates → vehicle branch / exact_waypoint-or-noSpline branch / probe-then-DoMovePoint branch; sets `LastMovement::Set(mapId,x,y,z,ori,delay,priority)`; reachability gate via GeneratePath rejects NOPATH straight-lines. OG semantics (long-path routing) live only in MoveTo2 | DEPS(T): MovementPriority, GeneratePath/PathResult, DoMovePoint, MoveDelay, config maxWaitForMove/disableMoveSplinePath | NOTES: **two parallel A→B systems now coexist in target** — native MoveTo (+TravelPlan/ExecuteTravelPlan) and ported MoveTo2 chain; NewRpg's MoveFarTo uses MoveTo2, combat/short actions use MoveTo. Port plan must decide the merge.

**MoveTo(WorldLocation) inline** | OG .h:28 | V .h:28 | delta: field renames only | TARGET: ABSENT (no WorldLocation overload) | NOTES: trivial.

**MoveTo(Unit*, distance)** | OG :2185 | V :1296 | delta: NONE | TARGET: DIVERGED T:969 `MoveTo(WorldObject*, distance, priority)` — different algorithm: clamps needToGo to spellDistance, interpolates dz then UpdateAllowedPositionZ; OG's hostile-target Stance-location redirect (AI_VALUE(Stance*,"stance")) is ABSENT in target | DEPS(OG): "stance" value, Formation::IsNullLocation, targetPosRecalcDistance | NOTES: OG moves to full target pos; target moves partway (max spellDistance per call).

**MoveNear(mapId,...)** | OG :47 | V :83 | delta: NONE | TARGET: PORTED-FAITHFUL T:285 (+priority param) | DEPS: GetFollowAngle | NOTES: none.

**MoveNear(WorldObject*, distance)** | OG :53 | V :89 | delta: NONE for CMANGOS branch (`GetObjectBoundingRadius`→`GetObjectSize`, `IsWithinLOS(...,true)`→no-flag — unmarked, drops "ignore M2 model" LOS flag) | TARGET: DIVERGED T:292 — same 8-angle sweep but: uses `GetCombatReach()`, computes raw circle points (no `GetNearPoint` ground/collision helper), adds `UpdateAllowedPositionZ` clamp per candidate; no `ai->GetRange("follow")` cap | DEPS: GetFollowAngle, LOS, MoveTo | NOTES: OG relies on GetNearPoint for Z/collision; target substitutes Z-clamp.

**GetFollowAngle** | OG :2238 | V :1349 | delta: NONE | TARGET: PORTED-FAITHFUL T:1070 + divide-by-zero guard for orphaned raid groups | DEPS: Group iteration | NOTES: none.

**Follow(target, distance)** | OG :2283 | V :1394 | delta: NONE | TARGET: PORTED-FAITHFUL T:1143 (default distance differs: config followDistance vs ai->GetRange("follow")) | NOTES: none.

**Follow(target, distance, angle)** | OG :2309 | V :1420 | deltas: (1) ADAPTATION(marked V:1477) — taxi-spline follow (`GetTaxiPathSpline`) stubbed/deferred; (2) **DEVIATION(unmarked)** V:1623 — `mm.MoveFollow(target, distance, angle, MOTION_SLOT_ACTIVE, sPlayerbotAIConfig.boostFollow)`: AC signature is `(target, dist, angle, MovementSlot, bool inheritWalkState, bool inheritSpeed)` (AC MotionMaster.h:230), so **boostFollow is fed into inheritWalkState**, not a speed boost — semantic mis-map; (3) water-transition block ported onto WorldPosition terrain queries (marked V:1496); (4) `IsSafe` fallback `MoveTo2(target)` — note OG :2312 same | TARGET: DIVERGED T:1312 — native rewrite: keeps corpse-follow, follower-of-bot long-move redirect (with extra 1.5x-closer + angle<π/2 condition OG lacks), taxi-follow via FlightPathMovementGenerator::GetPath (this bit is MORE faithful than vendored, which stubbed it); DROPS: FollowOnTransport, water-transition block, free-fly take-off/land packets, "follow jump" shortcut, chase-param dedup (angle/offset compare), boostFollow; adds followDistance early-outs (three redundant checks T:1319/1445/1455), mount distance+=angle oddity T:1452-1453 (**looks like a bug: adds an angle to a distance**) | DEPS(OG): PAI_VALUE("last long move"), FollowOnTransport, water terrain, "follow jump" strategy, GetChaseTarget/Angle/Offset facade, boostFollow config | NOTES: OG returns false when already following same target/angle/offset (dedup); target dedups on GUID only.

**FollowOnTransport** | OG :2693 | V :1800 | delta: NONE (`t_guid`→`transport.guid`, heartbeat shim) | TARGET: ABSENT (Follow rewrite lacks it; FollowActions/BoardTransport partially cover) | DEPS: m_movementInfo.transport.guid, Transport Add/RemovePassenger, Relocate+heartbeat | NOTES: needed for master-on-boat follow.

**ChaseTo** | OG :2538 | V :1644 | delta: ADAPTATION(marked) vehicle API; `Clear(false,true)`→`Clear(false)`; `MovePath`→`MoveSplinePath`; `SetTarget(obj)`→`SetTarget(obj->GetGUID())`; rest NONE (hazard-dodge geometry, arena MoveNear, behind-strategy angle, chase dedup, EFFECT_MOTION charge guard, jump::chase, `angle > 20` reset) | TARGET: DIVERGED T:1491 — minimal native: vehicle branch (MoveChase 30y), stand/interrupt, unconditional `MoveChase(obj, distance)`, WaitForReach(dist2d−distance). Missing: hazard avoidance, arena special-case, behind angle, dedup, charge guard, jump shortcut, `IsSafe` guards | DEPS(OG): HazardPosition value, IsValidPosition, getPathTo, GeneratePathAvoidingHazards, sServerFacade chase accessors | NOTES: target ReachCombatTo (T:1006) is the combat approach used by ReachTargetActions instead.

**MoveDelay** | OG :2688 | V :1795 | delta: NONE | TARGET: DIVERGED T:1531 — swim/flight/backwards speed-aware (improvement) | NOTES: used by WaitForReach + teleport throttles.

**WaitForReach(float)** | OG :2725 | V :1832 | delta: NONE | TARGET: PORTED-FAITHFUL T:1551 (`SetDuration`→`SetNextCheckDelay`; GCD clamp deliberately kept commented in all three) | DEPS: MoveDelay, config reactDelay/maxWaitForMove | NOTES: none.

**WaitForReach(PointsArray)** | OG :2742 | V :1849 | delta: NONE | TARGET: ABSENT (no caller in target) | NOTES: trivial sum-of-segments wrapper.

**Flee(Unit*)** | OG :2759 | V :1866 | deltas: (1) ADAPTATION(marked V:1884) ThreatManager returns Unit* directly; (2) **ADAPTATION(marked V:2016-2023)** — the solo-bot kite branch (`mm->MoveChase(target, fleeDist, ...)`) is disabled with `if (false && ...)` because AC ChaseMovementGenerator never increases distance (frozen-flee fix); falls through to FleeManager; (3) `IsWithinLOSInMap(target,true)`→no-flag (unmarked, M2 flag) | TARGET: DIVERGED T:1582 — native rewrite: tank-flee when bot is current victim, healer/ranged grouping else, spareTarget catch-all, random pick, FleeManager fallback with `GetRange("flee")` (no fullDistance×2 overworld/vs-player doubling), no hunter-kite fleeDelay=1, no flee/guard emote sounds, no "has area debuff" group-member filter, `possibleTargets.push_back(fleeTarget)` in spare branch pushes possibly-null/wrong unit (**target-side bug**, T:1705) | DEPS: FleeManager (exists in both), LastMovement.lastFlee, "health" value, IsTank/IsHeal/IsRanged, config fleeingEnabled/returnDelay | NOTES: OG minFlee=5y/maxFlee=isTarget?40:spell*1.5/minRangedTargetDistance=spell/2+follow — target approximates with shoot-range maths.

**Flee callers**: **FleeAction::Execute** OG :3155 | V :2271 | NONE | T:2002 faithful (+isUseful T:2007 added: requires "current target"). **FleeWithPetAction::Execute** OG :3160 | V :2276 (marked: AC SetReactState on Creature) | T:2019 faithful. **RunAwayAction::Execute** OG :3176 ("master target") | V :2292 NONE | T:2796 DIVERGED — flees "group leader" instead of master.

**ClearIdleState** | OG :2989 | V :2105 | delta: NONE | TARGET: PORTED-FAITHFUL T:1763 | DEPS: "stay time", "position"["random"] | NOTES: none.

**UpdateMovementState** | OG :2290 | V :1401 | delta: NONE (flag renames) | TARGET: DIVERGED T:1145 — full native rewrite: waterwalk aura handling, master-mirroring for swim/fly flags, CC-restriction tracking (`wasMovementRestricted`), SendMovementFlagUpdate batching; OG merely toggled MOVEFLAG_SWIMMING + UpdateSpeed(MOVE_SWIM/FLIGHT) | DEPS(T): GetLiquidData, aura checks, master flags | NOTES: OG's unconditional `UpdateSpeed(MOVE_SWIM, true)` every call is dropped.

**IsMovingAllowed(Unit*)** | OG :2259 | V :1370 | delta: NONE | TARGET: DIVERGED T:1097 — reactDistance gate commented out (map check + CanMove only) | NOTES: target bots will pursue beyond reactDistance.

**IsMovingAllowed(mapId,x,y,z)** | OG :2274 | V :1385 | delta: NONE | TARGET: ABSENT — replaced by parameterless `IsMovingAllowed()` T:1138 (= botAI->CanMove()).

**isPossible / isUseful (MovementAction base)** | OG :37/:42 | V :73/:78 | delta: NONE | TARGET: ABSENT on base class — "stay" gate reimplemented per-subclass (e.g. ReachTargetAction::isUseful); base MovementAction has no overrides | NOTES: port plan must ensure every movement action in target still respects `stay` + CanMove.

**CreateWp** | OG :22 | V :58 | delta: NONE (TEMPSPAWN→TEMPSUMMON) | TARGET: PORTED-FAITHFUL T:250 (AddAura via `bot->AddAura(246, wp)`) | NOTES: debug-only.

**GetAngle(x1,y1,x2,y2)** | OG .h:80 declared, **never defined** | V .h:80 same | delta: NONE (both dead declarations) | TARGET: ABSENT | NOTES: drop from port plan.

**lastMoveShort handling** | OG: field written only in dead MoveTo body (:2176) and read in dead code + `bot_movement.csv` logging; live MoveTo2 path never updates it | V: same (dead code removed ⇒ never written) | TARGET: actively used — `LastMovement::Set()` stores it on every native MoveTo; `IsDuplicateMove` (T:1112) compares against it with 0.01y + maxWaitForMove window; `setShort()` exists | NOTES: in target, MoveTo2 bypasses lastMoveShort entirely → mixing MoveTo2 and MoveTo means duplicate-move suppression only sees native moves.

**stop/StopMoving semantics** | OG: `ai->StopMoving()` (PlayerbotAI wrapper: interrupts + clears) for bot; `mover->InterruptMoving(true)` for vehicle | V: `ai->StopMoving()` kept; vehicle → `mover->StopMoving()` (unmarked — AC lacks InterruptMoving) | TARGET: `bot->StopMoving()` directly in MoveTo2 (T:4060); no PlayerbotAI::StopMoving wrapper in that path | NOTES: verify target StopMoving doesn't cancel the spline the same tick DispatchMovement starts one (OG wrapper had ordering guarantees).

## D. JumpAction cluster (all OG/V line pairs; TARGET: entire cluster ABSENT — target `JumpTo` T:265 is a trivial `MotionMaster::MoveJump` unrelated to this system)

- **JumpAction::isUseful** OG :3420 | V :2531 | NONE.
- **JumpAction::Execute** OG :3425 | V :2536 | deltas: ADAPTATION(marked V:2658, V:2688) — the "already following/chasing this target" guards became `false` because AC generators don't expose current target → **guard disabled** (bot may jump while genuinely mid-follow of the same target); `orientation`→`m_orientation`; sLog→sBotLog | DEPS: RTSC values ("RTSC saved location" jump/jump point), "position" map, Formation, config jumpInPlaceChance/jumpBackwardChance/jumpHSpeed/jumpVSpeed, strategies follow/wander/stay/rtsc jump.
- **CalculateJumpParameters** OG :3747 | V :2858 | delta: ADAPTATION(marked) — cmangos `Map::GetHitPosition` → `BotMapHitPos` (V:33, static+dynamic tree GetObjectHitPos nearest-hit merge) under MANGOSBOT_TWO (active); non-TWO branches stubbed false (dead). Note OG's non-TWO second probe uses `oz + 0.5f` (OG :3848) — asymmetry not replicated (dead branch anyway) | DEPS: GetCollisionHeight/Width, UpdateAllowedPositionZ, IsJumpSafe, gravity const 19.2911, config jumpHeightLimit.
- **CalculateJumpTime(5-arg)** OG :3942 | V :3053 | NONE. **CalculateJumpTime(3-arg)** OG :3968 | V :3079 | NONE.
- **IsJumpSafe** OG :3990 | V :3101 | NONE. **CanWalkTo** OG :3995 | V :3106 | NONE (getPathStepFrom dep). **IsJumpFasterThanWalking** OG :4024 | V :3135 | NONE. **CanLand** OG :4056 | V :3167 | NONE (Map::GetHeight signature). **IsNotMagmaSlime** OG :4072 | V :3183 | ADAPTATION(marked) — AC `Map::GetLiquidData` + flags. **CanJumpTo** OG :4097 | V :3202 | NONE. **JumpTowards** OG :4135 | V :3240 | NONE.
- **DoJump** OG :4170 | V :3275 | delta: ADAPTATION(marked ×4) — packed-guid double-write fix (`m_movementInfo.guid = bot->GetGUID(); BotWriteMoveInfo(...)`; OG wrote guid explicitly then movementInfo — on AC that crashed the server); knock-back send via `SendMessageToSet(&data, bot)` vs OG `GetMover()->SendMessageToSetExcept(data, bot)` (unmarked delta: target of exclusion semantics equivalent, but sender is bot not mover); `sWorld.GetAverageDiff()`→`sWorldUpdateTime.GetAverageUpdateTime()`; MOVEFLAG_JUMPING(zero)→FALLING both branches | DEPS: ai->SetFallAfterJump/SetJumpDestination/SetJumpTime/QueuePacket, SetFallInformation, MSG_MOVE_JUMP/START_FORWARD/BACKWARD/KNOCK_BACK.
- **GetPossibleJumpStartFor** OG :4282 | V :3391 | NONE. **GetPossibleJumpStartForInRange** OG :4349 | V :3458 | ADAPTATION(marked V:3515) — `Map::GetReachableRandomPointOnGround` → ported `WorldPosition::GetReachableRandomPointOnGround` (note memory: that WorldPosition fn was still stubbed as of closest-correct-point work — **verify before trusting**; if still stubbed, vendored in-range jump search never finds points and silently falls back to pathfinder variant).

## E. MoveToTravelTargetAction / MoveToRpgTargetAction

**MoveToTravelTargetAction::Execute** | OG-TT :14 | V-TT :14 | deltas: (1) ADAPTATION(marked V:29-71) — **position-based travel stuck detector**: thread_local per-guid map, 6 consecutive executions with <4y XY movement → teleport to dest (UpdateAllowedPositionZ + `tz > INVALID_HEIGHT` sentinel guard) when no player within sightDistance of bot or dest; resets retry; (2) ADAPTATION(marked V:199-229) — on `IsMaxRetry(true)` teleport (same player-guards + Z guard) instead of immediately COOLDOWN; falls back to COOLDOWN+SetForced(false) if teleport declined; (3) group-leader wait/emote block, radiusMin scatter (angle=2π·urand/100, mod 50-100%), IncRetry/DecRetry(true), debug Poi — all NONE | TARGET: **ABSENT** — no TravelTarget framework on this branch; functional analog = NewRpg layer: `NewRpgBaseAction::MoveFarTo` (NRB:50, straight MoveTo2 wrapper) + rpgInfo status machine + MoveTo2's unstick-step + SafeBotTeleport far-teleport. **The retry-counter → cooldown → teleport valve has no direct target equivalent**; nearest analogs are MarkUnreachable/IsMarkedUnreachable (NRB:62-71, 60s GO blacklist) and ResolveMovePath's empty-path policy | DEPS(OG/V): TravelTarget (GetStatus/SetStatus/CheckStatus/GetPosition/GetDestination→GetRadiusMin/GetTimeLeft/SetExpireIn/Inc-Dec-IsMaxRetry/SetRetry/IsForced/GetConditions), TravelStatus enum, "travel target" value, maxWaitForMove | NOTES: retry channel is the boolean-flagged "move" counter (`true` arg); OG only distinguishes move-vs-target retries in debug text.

**MoveToTravelTargetAction::isUseful** | OG-TT :191 | V-TT :262 | delta: **DEVIATION(unmarked)** — OG gates re-issue with `bot->IsMovingIgnoreFlying()` (non-ZERO builds); vendored uses `bot->isMoving()` — a flying-flagged moving bot is treated as moving in vendored but not OG (minor; affects re-dispatch while airborne). Rest NONE: AllowActivity(TRAVEL_ACTIVITY), "travel target traveling" value, taxi check, current-position LastChangeDelay<10, "can move around", guild-order condition skip under follow/wander, non-leader follow/stay/guard block unless forced, dungeon-leader group-gather gate via `sTravelMgr.MapTransDistance`, loot-possible bail, CanFreeMoveValue::CanFreeMoveTo | TARGET: ABSENT (gating spread across NewRpg triggers/strategies) | DEPS: FreeMoveValues, LootObjectStack, TravelMgr::MapTransDistance, MEM_AI_VALUE current position.

**MoveToRpgTargetAction::Execute** | OG-RT :14 | V-RT :14 | deltas: (1) **ADAPTATION(marked V:169-202)** — stand-point selection replaced: OG picked ONE angle (random full-circle when no LOS) then `movePos.ClosestCorrectPoint(5,5,instanceId)` + `|Δz|>10` check with NO LOS validation of the result; vendored sweeps 8 offsets {0, ±π/4, ±π/2, ±3π/4, π} from baseAngle, requires ClosestCorrectPoint + Δz≤10 + `wo->IsWithinLOS(candidate + collisionHeight)`, far candidates (≥sightDistance) accepted unvalidated; explicitly mirrors mod_playerbots MoveWorldObjectTo; (2) ADAPTATION(marked V:148-157) — in-range distance normalised `/INTERACTION_DISTANCE` (OG bug: kept raw yards then multiplied by INTERACTION_DISTANCE again → pushed bot back out to ~5.5×current distance); npc stand distance tuned frand(0.36-0.73) vs OG frand(0.5-1) (marked deliberate 2-4y tuning); (3) ADAPTATION(marked V:216-218) — `GetInteractionPauseTimer`/`PauseWaypoints` deferred (AC lacks API) → moving NPCs are NOT paused; (4) `IsOutdoors` walk-mode check inverted source: OG `!bot->GetTerrain()->IsOutdoors(...)` adds WALK when **indoors**; vendored `bot->IsOutdoors()` adds WALK when **outdoors** — **DEVIATION (marked as AC API swap but the polarity flipped!)** V:132 vs OG :132; (5) drop valves all NONE: player-target mutual-rpg drop, moving-target 1-in-21 near drop, CanFreeMoveTo drop, >reactDistance×2 drop, GO under/above 1-in-6 drop, 1-in-51 random drop, could-not-move drop (each inserts into "ignore rpg target" + RESET "rpg target") | TARGET: **ABSENT** as an action — analog `NewRpgBaseAction::MoveWorldObjectTo` (NRB:73): 8-angle deterministic sweep ×4 passes (LOS+flat / LOS+steep / noLOS+flat / noLOS+steep), direct `findNearestPoly` snap (5y extents, Δz≤10) with NAV_GROUND_STEEP/WATER/MAGMA/SLIME filters = the ClosestCorrectPoint port, ring pulled to `max(distance-2, 2.5)`, LOS as preference not hard-reject, then MoveFarTo; unreachable valve = MarkUnreachable 60s blacklist rather than "ignore rpg target" set | DEPS(OG/V): "rpg target" GuidPosition value, "ignore rpg target" set, PAI_VALUE mutual check, CanFreeMoveValue, ClosestCorrectPoint, config RandombotsWalkingRPG, INTERACTION_DISTANCE | NOTES: vendored fixed genuine OG bugs here (double-multiply distance; LOS-less snap parking bots outside walls) — port plan should carry the vendored semantics, not OG's, but the walk-mode polarity flip needs a decision (OG intent: walk indoors for immersion).

**MoveToRpgTargetAction::isUseful** | OG-RT :221 | V-RT :257 | delta: NONE (API case) | TARGET: ABSENT (NewRpg triggers cover: travel-traveling gate, distance<INTERACTION_DISTANCE gate, "can move around", loot-possible bail, moving-and-not-follow gate with 60s position-stall bypass) | NOTES: OG null-wo branch resets value but **falls through without returning** (OG :231-239) — executes rest with dangling logic; vendored inherits same quirk (V:267-275).

## F. ReachTargetActions link

**ReachTargetAction (Qualified)** | OG ReachTargetActions.h:15 | V same :15 | delta: DEVIATION(unmarked but API-shaped): `GetDistance(target,false,DIST_CALC_COMBAT_REACH)`→`GetDistance(target)` (AC GetDistance subtracts combat reach — approx equivalent), `IsWithinLOSInMap(target,true)`→no-flag, `IsNonMeleeSpellCasted`→`IsNonMeleeSpellCast` | TARGET: DIVERGED — T ReachTargetActions.h:14/.cpp:13: no Qualified spell-range plumbing, no MoveStyleValue::WaitForEnemy standoff, no MoveNear-vs-ChaseTo friend/LOS split; just `ReachCombatTo(target, distance)` + IsWithinCombatRange isUseful + channeled-cast guard | DEPS(OG): GetSpellRange, MoveStyleValue, PullStrategy (ReachPullAction), CanCastSpell | NOTES: OG subclasses ReachMelee/ReachSpell/ReachPull/ReachPartyMemberToHeal/ReachPartyMemberForTotem; target has ReachMelee/ReachSpell/ReachPartyMemberToHeal/ReachPartyMemberToResurrect (no Pull — pull handled elsewhere; no Totem variant).

## G. Target-only members in scope files (no OG/V counterpart — must be reconciled by the port plan)

- **EmitDebugMove** T:75 — rich `debug move` tracer (RPG status, quest objective naming, travel-plan next hop, step vs goal distance). Replaces OG TellDebug/CreateWp narration.
- **SafeBotTeleport** (anon ns) T:60 — Z-conform + `-2000`/IsValidMapCoord rejection before TeleportTo. Used by MoveOffTransport/HandleSpecialMovement/MoveTo2.
- **GeneratePath/PathResult** T:1950, **DoMovePoint** T:1963, **IsDuplicateMove** T:1112, **IsWaitingForLastMove** T:1124, **SetNextMovementDelay** T:1575, **MovementPriority** enum — target's short-move gating/probing layer; OG has none of it.
- **TravelPlan pipeline**: GetTravelPlan T:3379, ExecuteTravelPlan T:4232, LaunchWalkSpline T:3185, CheckSplineProgress T:3140, MoveToSpline T:3344, RefineWalkPoints T:3296, plus transport helpers GetTransportForPosTolerant T:4595, FindBoardingPointOnTransport T:4609, BoardTransport T:4656 — the branch's earlier node-graph executor, running **in parallel** with the ported MoveTo2 chain (NewRpgInfo.travelPlan vs LastMovement.lastPath).
- **ReachCombatTo** T:1006, MoveAway T:1769, MoveFromGroup/Move/MoveInside, BestPositionForMeleeToFlee/BestPositionForRangedToFlee/FleePosition/CheckLastFlee, AvoidAoeAction (3 sub-avoiders), CombatFormationMoveAction, TankFaceAction, RearFlankAction, DisperseSetAction, RotateAroundTheCenterPointAction, MoveAwayFromCreature/PlayerWithDebuff — target-native combat-movement family occupying the space of OG's hazard system + stances.
- **MoveOutOfCollisionAction** T:2904: DIVERGED — lost the OG GetReachableRandomPointOnGround retry loop (only the "old style" fallback remains; note the `M_PI * 2000 / frand(1,1000)` angle formula, an OG oddity preserved). **MoveRandomAction** T:2922: DIVERGED — 3-try collision/water-checked sampler; isUseful now `!rpg target` vs OG population-based. **SetBehindTargetAction** T:2844: DIVERGED — full rewrite on CombatFormationMoveAction with FleeInfo history. **SetFacingTargetAction** T:2818: faithful + reactDelay. **MoveToLootAction** T:2798: simplified (no LOS split / debug narration). **MoveToAction (qualified GuidPosition list)** OG :3408 | V :2519 | NONE | TARGET: ABSENT.

## DEPENDENCY SUMMARY — what Scope A needs from other subsystems

1. **TravelPath/PathNodePoint** (TravelNode.h): getPath/getPointPath/getBack/getFront/addPath/addPoint/clear/empty/makeShortCut/**UpcommingSpecialMovement**/**ClipPath**/cutTo, PathNodeType {NODE_PATH, NODE_NODE, NODE_PREPATH, NODE_FLIGHTPATH, NODE_TRANSPORT, NODE_TELEPORT, NODE_STATIC_PORTAL, NODE_AREA_TRIGGER}. Target already carries a port (ClipPath/makeShortCut present) — Scope B/C must verify fidelity.
2. **sTravelNodeMap.getFullPath(start,end,bot)** + getNodes + (target-only) GetLastRouteFailReason; OG debug path also uses getRoute/cleanTempNodes/m_nMapMtx.
3. **WorldPosition** helpers — the biggest gap driver. Used here: distance/sqDistance/sqDistance2d/fDist/getPathTo/getPathStepFrom/getPathLength/fromPointsArray/toPointsArray/getVector3/getAngleTo/getAngleBetween/limit/isValid; water: isInWater/isUnderWater/**setAtWaterSurface**/getWaterLevel/getGroundLevel; flight: **isOutside/canFly/currentHeight/getHeight**; misc: **ClosestCorrectPoint**/**GetReachableRandomPointOnGround**/RandomPointOnTrans/getTransports/isOnTransport/closestSq/isOverworld/isDungeon/getAreaName/canPathTo/IsInLineOfSight/printWKT. Target absents (flight set, setAtWaterSurface, hazard geometry) are exactly what keeps FlyDirect/UpdateFlyingState/hazards stubbed.
4. **LastMovement value**: lastPath, lastMoveShort, nextTeleport, lastFlee, lastAreaTrigger, lastTransportEntry, **moveEvent (missing in target)**, Set/setPath/setShort/clear, plus target-only msTime/lastdelayTime/priority.
5. **AI values**: "last movement", "last area trigger", "travel target"(+traveling), "rpg target"/"ignore rpg target", "hazards" (**absent in target**), "stance" (**not consumed by target MoveTo**), "possible attack targets" (via ClipPath), "nearest npcs"/"nearest game objects", "current/master/pull target", "loot target"/"available loot"/"has available loot", "can move around", "current position" MEM value, "last long move", "position" map, "collision"/"inside target"/"facing", "has reagents for" (**dropped in target's HandleSpecialMovement**), "current mount speed" (dropped), RTSC values, "formation", "follow target".
6. **PlayerbotAI services**: CanMove, StopMoving (wrapper absent in target path), AllowActivity(DETAILED_MOVE_ACTIVITY/TRAVEL_ACTIVITY/plain), HasPlayerNearby(pos,range), Unmount/RemoveShapeshift/InterruptSpell, DoSpecificAction("cast"/"hearthstone"/"jump::*"), QueuePacket, SetJumpTime/SetJumpDestination/SetFallAfterJump/IsJumping, HasCheat, GetLastEvent (**no target equivalent**), TellDebug/TellPlayerNoFacing/Poi, IsSafe/IsRealPlayer/HasRealPlayerMaster/HasActivePlayerMaster, GetRange("follow"/"spell"/"flee"/"heal"/"shoot").
7. **Core/engine**: MotionMaster MoveSplinePath (AC Escort gen — load-bearing for the DispatchMovement fix), MovePoint(forcedMovement,speed,generatePath), MoveFollow **6-param signature (boostFollow mis-map!)**, MoveChase, Clear(reset) (no 2-arg Clear in AC); PathFinder→PathGenerator shim (botcompat.h:315); Map collision (`GetMapCollisionData` static+dynamic GetObjectHitPos — the BotMapHitPos pattern, needed if JumpAction is ever ported); Transport Add/RemovePassenger/CalculatePassengerPosition/Offset; taxi (sTaxiPathStore, ActivateTaxiPathTo, CleanupAfterTaxiFlight); UpdateAllowedPositionZ; VMAP_INVALID_HEIGHT sentinel handling (SafeBotTeleport).
8. **FleeManager** (playerbot/FleeManager.*): isUseful/CalculateDestination — present in OG, V, and target.
9. **Config keys**: targetPosRecalcDistance, reactDistance, sightDistance (**replaced by hardcoded 50y in target ResolveMovePath**), reactDelay, maxWaitForMove, passiveDelay, walkDistance, contactDistance, spellDistance, lootDistance, fleeDistance/followDistance/tooCloseDistance/meleeDistance (target), transportTeleportType, enableMinimalMove, fleeingEnabled, returnDelay, boostFollow, RandombotsWalkingRPG, jumpVSpeed/jumpHSpeed/jumpInPlaceChance/jumpBackwardChance/jumpHeightLimit, disableMoveSplinePath (target-only), tweakValue.
10. **Cross-subsystem behaviors to preserve**: travel retry/cooldown/teleport valve (TravelTarget — Scope owning TravelMgr), CanFreeMoveValue::CanFreeMoveTo, LootObjectStack::GetLoot precedence over travel/rpg movement, ChooseRpgTargetAction's "ignore rpg target" set lifecycle, PullStrategy range for ReachPull, MoveStyleValue::WaitForEnemy, HazardsValue (whole subsystem absent in target), PerformanceMonitor (MinimalMove), sBotLog/sLog debug channels.

**Highest-risk findings for the port plan**: (1) target runs two competing A→B systems (TravelPlan executor vs ported MoveTo2) — unify or define ownership; (2) MoveTo2 bypasses target's MovementPriority/IsDuplicateMove/lastMoveShort gating; (3) `moveEvent` and travel retry/cooldown valve unported; (4) MinimalMove (offline path-walking teleports) absent; (5) FlyDirect/UpdateFlyingState/hazards/JumpAction wholly stubbed or absent; (6) vendored `MoveFollow` boostFollow→inheritWalkState mis-map; (7) vendored WaitForTransport `.front()` on possibly-empty path (fixed only in target); (8) HandleSpecialMovement's dropped reagent/spell-ready gates in target; (9) MoveToRpgTarget walk-mode indoor/outdoor polarity flip in vendored; (10) `GetReachableRandomPointOnGround` (WorldPosition port) may still be stubbed — verify before relying on MoveOutOfCollision/jump-in-range paths.
